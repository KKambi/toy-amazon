doctype html
html
  head
    meta(charset='UTF-8')
    meta(name="viewport" content="width=device-width")
    meta(name="description" content="Amazon Carousel")
    meta(name="author" content="KKambi")
    title= title
  body
    div#header
        div
            if signed
                | #{name}님 환영합니다
                | (#{user_id})
        div#sign-container
            a(href="./sessions/new") 로그인
            a(onclick="document.querySelector('#login-form').submit();") 로그아웃
            form#login-form(action="/sessions/destroy" method="POST" style="display:none;")
    div#main-card-continaer.container
    div#main-beneift-container.container
    div#video-benefit-container.container
    div#music-benefit-container.container
    script(src="/javascripts/index.bundle.js")